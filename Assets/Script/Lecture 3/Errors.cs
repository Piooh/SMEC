using UnityEngine;
using System.Collections;

public class Errors : MonoBehaviour 
{
	private void Awake()
	{
		// 컴파일 에러
		//int value = 1
		//Debug.Log( value );

		// 런타임 에러
		//var type = Input.GetAxis( "Horizonttal" ).GetType();
		//Debug.Log( data );
	
		// 논리 에러
	}
}
